diff '--color=auto' -ruN chromium-62.0.3202.94_a/chrome/browser/extensions/component_extensions_whitelist/whitelist.cc chromium-62.0.3202.94_b/chrome/browser/extensions/component_extensions_whitelist/whitelist.cc
--- chromium-62.0.3202.94_a/chrome/browser/extensions/component_extensions_whitelist/whitelist.cc	2017-11-13 13:09:13.000000000 -0700
+++ chromium-62.0.3202.94_b/chrome/browser/extensions/component_extensions_whitelist/whitelist.cc	2017-11-20 14:03:19.773333998 -0700
@@ -30,8 +30,6 @@
 
 bool IsComponentExtensionWhitelisted(const std::string& extension_id) {
   const char* const kAllowed[] = {
-    extension_misc::kHotwordSharedModuleId,
-    extension_misc::kInAppPaymentsSupportAppId,
     extension_misc::kMediaRouterStableExtensionId,
     extension_misc::kPdfExtensionId,
 #if defined(OS_CHROMEOS)
diff '--color=auto' -ruN chromium-62.0.3202.94_a/chrome/browser/extensions/component_loader.cc chromium-62.0.3202.94_b/chrome/browser/extensions/component_loader.cc
--- chromium-62.0.3202.94_a/chrome/browser/extensions/component_loader.cc	2017-11-13 13:09:13.000000000 -0700
+++ chromium-62.0.3202.94_b/chrome/browser/extensions/component_loader.cc	2017-11-20 13:54:32.796688113 -0700
@@ -493,11 +493,6 @@
     Add(IDR_BOOKMARKS_MANIFEST,
         base::FilePath(FILE_PATH_LITERAL("bookmark_manager")));
   }
-#if BUILDFLAG(ENABLE_PRINTING)
-  // Cloud Print component app. Not required on Chrome OS.
-  Add(IDR_CLOUDPRINT_MANIFEST,
-      base::FilePath(FILE_PATH_LITERAL("cloud_print")));
-#endif  // BUILDFLAG(ENABLE_PRINTING)
 #endif  // defined(OS_CHROMEOS)
 
   if (!skip_session_components) {
@@ -575,13 +570,6 @@
     AddHotwordAudioVerificationApp();
     AddHotwordHelperExtension();
     AddImageLoaderExtension();
-
-    bool install_feedback = enable_background_extensions_during_testing;
-#if defined(GOOGLE_CHROME_BUILD)
-    install_feedback = true;
-#endif  // defined(GOOGLE_CHROME_BUILD)
-    if (install_feedback)
-      Add(IDR_FEEDBACK_MANIFEST, base::FilePath(FILE_PATH_LITERAL("feedback")));
   }
 
 #if defined(OS_CHROMEOS)
diff '--color=auto' -ruN chromium-62.0.3202.94_a/chrome/browser/extensions/external_component_loader.cc chromium-62.0.3202.94_b/chrome/browser/extensions/external_component_loader.cc
--- chromium-62.0.3202.94_a/chrome/browser/extensions/external_component_loader.cc	2017-11-13 13:09:13.000000000 -0700
+++ chromium-62.0.3202.94_b/chrome/browser/extensions/external_component_loader.cc	2017-11-20 13:59:04.716677390 -0700
@@ -38,12 +38,6 @@
 
 void ExternalComponentLoader::StartLoading() {
   prefs_.reset(new base::DictionaryValue());
-#if defined(GOOGLE_CHROME_BUILD)
-  AddExternalExtension(extension_misc::kInAppPaymentsSupportAppId);
-#endif  // defined(GOOGLE_CHROME_BUILD)
-
-  if (HotwordServiceFactory::IsHotwordAllowed(profile_))
-    AddExternalExtension(extension_misc::kHotwordSharedModuleId);
 
 #if defined(OS_CHROMEOS)
   {
@@ -59,12 +53,6 @@
     AddExternalExtension(extension_misc::kMediaRouterStableExtensionId);
   }
 
-#if BUILDFLAG(ENABLE_APP_LIST) && defined(OS_CHROMEOS)
-  std::string google_now_extension_id;
-  if (GetGoogleNowExtensionId(&google_now_extension_id))
-    AddExternalExtension(google_now_extension_id);
-#endif
-
   LoadFinished();
 }
 
